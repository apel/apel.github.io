language: ruby
rvm: 2.1

sudo: false

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true  # speeds up installation of html-proofer

install:
  - gem install jekyll
  - gem install html-proofer

script:
  - jekyll build
  - htmlproof ./_site
